# Digital Marketing — Mobile Site

Simple, mobile-friendly website for a Digital Marketing studio in Nepal.

Preview locally

```bash
# from the project root (this folder)
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```

Replace placeholders

- Update the WhatsApp link in `index.html` (search for `wa.me/977XXXXXXXXX`) with your full number.
- Update the contact form `mailto:` address in `index.html`.
# Hub-It
